<Window x:Class="Secret_Santa_Generator.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Secret_Santa_Generator"
        xmlns:viewModel="clr-namespace:Secret_Santa_Generator.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModel:MainViewModel, IsDesignTimeCreatable=False}"
        WindowStartupLocation="CenterScreen"
        Title="MainView" Height="450" Width="400">
    <Grid KeyDown="UIElement_OnKeyDown">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding NextId, UpdateSourceTrigger=PropertyChanged}" FontSize="100" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Button Grid.Row="1" Content="Hide!" FontSize="40" Width="300" Height="60" Margin="5"
                    Command="{Binding HideCommand}"/>
            <Button Grid.Row="1" Content="Get next ID" FontSize="40" Width="300" Height="100" Margin="5"
                    Command="{Binding GetNextIdCommand}"/>
        </StackPanel>
    </Grid>
</Window>
